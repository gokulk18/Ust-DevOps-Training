apiVersion: v1
kind: Service
metadata:
  name: mono-service
  labels:
    app: mono-service

spec: 
  type: NodePort
  selector: 
    app: mono-app

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30005
